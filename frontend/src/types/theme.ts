export interface DashboardTheme {
  isDark: boolean;
  bg: string;
  bgMuted?: string;
  sidebarBg: string;
  cardBg: string;
  text: string;
  textMuted: string;
  border: string;
  accent: string;
  primary: string;
  primaryLight?: string;
  success: string;
  danger: string;
}
